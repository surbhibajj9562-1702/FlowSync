# FlowSync — learning project
# Used by docker-compose and local dev

# PostgreSQL (Docker default)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=flowsync

# Optional: OpenAI for workflow summaries and insights
# OPENAI_API_KEY=sk-...
