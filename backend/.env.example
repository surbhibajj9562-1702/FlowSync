# FlowSync backend — learning project
# Copy to .env and fill in values

# Required: PostgreSQL connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/flowsync

# Optional: API port (default 4000)
# PORT=4000

# Optional: OpenAI API key for workflow summaries and execution insights
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
